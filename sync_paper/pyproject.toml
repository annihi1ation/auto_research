[project]
name = "auto-research"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kagglehub",
    "pydantic-ai",
    "pydantic-graph",
    "fastapi",
    "uvicorn",
]
